//Okay before this method works properly you will need to instantiate a variable as follows at the top of the class
    // private boolean whiteToMove = true;
    //also add to the newGame() method a step at the end that goes
    // whiteToMove = true;
    //also at the end of the nextMove() method, add a step as follows
    // whiteToMove = !whiteToMove;
    public String getTurn()
    {
        if(whiteToMove)
            return "White";
        else
            return "Black";
    }
